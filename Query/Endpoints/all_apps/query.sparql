BASE <https://id.volunteeringdata.io/>
PREFIX : <https://ns.volunteeringdata.io/>

SELECT *
WHERE {
    ?app
        :appDescription ?description ;
        :appEcosystem ?ecosystem ;
        :appLogo ?logo ;
        :appName ?name ;
        :appOrganisation ?organisation ;
    .

    OPTIONAL { ?app :appBrandColor ?brandColor . }
}
