BASE <https://id.volunteeringdata.io/>
PREFIX : <https://ns.volunteeringdata.io/>

SELECT *
WHERE {
    ?measurementUnit
        :measurementUnitCategory ?unitCategory ;
        :measurementUnitPluralLabel ?unitPluralLabel ;
        :measurementUnitSingularLabel ?unitSingularLabel ;
    .
}
