BASE <https://id.volunteeringdata.io/>
PREFIX : <https://ns.volunteeringdata.io/>

CONSTRUCT {
    <Causes> :causes ?cause .

    ?cause
        :label ?label ;
    .
}
WHERE {
    ?cause
        a :Cause ;
        :label ?label ;
    .
}
